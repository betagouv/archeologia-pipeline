__pycache__/
*.pyc
.vscode/
.claude/
.superpowers/

# PyInstaller (dev/runner_onnx build artifacts)
dev/runner_onnx/build/
dev/runner_onnx/dist/
dev/runner_onnx/.venv_*/

# Virtual environments
.venv/
.venv_*/
venv/
env/
ENV/

# Python tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# User config (chemins personnels, jamais versionné — voir config.example.json)
config.json
last_ui_config.json
class_color_registry.json

# Rapports d'audit internes — NE JAMAIS pousser (vulnérabilités résiduelles +
# chemins personnels ; le dépôt distant est public).
AUDIT*.md

# Documentation locale non versionnée (docs utilisateur .docx, runbook OVH avec
# login/chemins perso, plans internes ; le dépôt distant est public).
dev/docs/_local/

# Logs
*.log
pipeline_log_*.txt

# Local outputs / temp
temp/
results/
output/
output_test/
dev/image_test/

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Data: exported temp zones (QGIS memory layers)
data/temp_zones/

# Data: compiled CV runner binaries
data/third_party/cv_runner_onnx/windows/*.exe
data/third_party/cv_runner_onnx/linux/*

# Data: models (large files, not versioned)
data/models/**
!data/models/.gitkeep

# Data: quadrillage France (large shapefile, not versioned)
data/quadrillage_france/

# Fichiers de mot de passe HTTP Basic (dépôt QGIS OVH) — jamais versionnés
htpasswd.txt
.htpasswd

# (Portage de calcul OVH déplacé vers un dépôt privé séparé.)
