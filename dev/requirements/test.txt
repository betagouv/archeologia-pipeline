# Tests & qualité de code
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4

# Lecture des args.yaml de modèles CV (model_config.py, model_profile.py)
# QGIS embarque PyYAML, mais les tests hors QGIS en ont aussi besoin pour
# reproduire les chargements de configuration modèle.
pyyaml>=6.0

# Géométrie : nécessaire pour tester pipeline.cv.postprocessing.
# QGIS l'embarque, mais les tests hors QGIS le requièrent. Optionnel
# côté plugin (les fonctions concernées font import lazy dans except).
shapely>=2.0

# DBSCAN du clustering (pipeline.cv.clustering : cKDTree) : nécessaire pour
# tester run_synthesis/run_clustering. QGIS l'embarque (dépendance de fait).
scipy>=1.10

# Lecture des métadonnées raster + interprétation CRS : nécessaire pour
# tester pipeline.tilespec (crs_is_projected, TileSpec.from_raster,
# assign_crs_if_missing) et pipeline.ingest_plan. Côté plugin l'accès
# géospatial passe par osgeo/GDAL (fourni par QGIS) ; rasterio est importé
# en lazy dans des except, donc absent = ces fonctions renvoient None.
rasterio>=1.3
