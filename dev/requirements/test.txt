# Tests & qualité de code
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4

# Lecture des args.yaml de modèles CV (model_config.py, model_profile.py)
# QGIS embarque PyYAML, mais les tests hors QGIS en ont aussi besoin pour
# reproduire les chargements de configuration modèle.
pyyaml>=6.0

# Géométrie : nécessaire pour tester pipeline.cv.postprocessing.
# QGIS l'embarque, mais les tests hors QGIS le requièrent. Optionnel
# côté plugin (les fonctions concernées font import lazy dans except).
shapely>=2.0
